apiVersion: v1
kind: Pod
metadata: 
  name: small-pod
  namespace: dev
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "200m"
          memory: "256Mi"

